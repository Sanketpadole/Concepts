package Collection3;

import IteratorVsListIterator.Basic;
import IteratorVsListIterator.List;
import IteratorVsListIterator.Set;
import IteratorVsListIterator.Traverse;
import IteratorVsListIterator.a;
import IteratorVsListIterator.as;
import IteratorVsListIterator.backwards;
import IteratorVsListIterator.can;
import IteratorVsListIterator.collection;
import IteratorVsListIterator.direction;
import IteratorVsListIterator.elements;
import IteratorVsListIterator.in;
import IteratorVsListIterator.is;
import IteratorVsListIterator.no;
import IteratorVsListIterator.obtain;
import IteratorVsListIterator.of;
import IteratorVsListIterator.only;
import IteratorVsListIterator.the;
import IteratorVsListIterator.to;
import IteratorVsListIterator.traverse;
import IteratorVsListIterator.using;

public class IteratorVsListIterator {

	Basic Iterator
	can traverse
	the elements
	in a
	collection only
	in forward
	direction.ListIterator can
	traverse the
	elements in
	a collection
	in forward
	as well
	as the
	backwards direction.
	Add Iterator
	is unable
	to add
	elements to
	a collection.
	ListIteror can
	add elements
	to a
	collection.Modify Iterator
	can not
	modify the
	elements in
	a collection.
	ListIterator can
	modify the
	elements in
	a collection

	using set().
	Traverse	Iterator can traverse Map, List and Set.	ListIterator can traverse List objects only.
	Index	Iterator has no method to obtain an index of the element in a collection.	Using ListIterator, you can obtain an index of the element in a collection

}
